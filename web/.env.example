NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Server-only — nunca expor no navegador
WORKER_URL=https://memopic-fdxa.onrender.com
WORKER_SECRET=changeme

# Server-only — service_role do Supabase (ignora RLS).
# Supabase → Project Settings → API → service_role key.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Server-only — Mercado Pago (Fase 4). Access Token de TESTE:
# Mercado Pago Developers → sua aplicação → Credenciais de teste.
MERCADOPAGO_ACCESS_TOKEN=TEST-...

# Server-only — configurar DEPOIS do deploy: Mercado Pago Developers → sua
# aplicação → Webhooks → URL https://<dominio>/api/webhooks/mercadopago →
# copiar a "Assinatura secreta" gerada.
MERCADOPAGO_WEBHOOK_SECRET=
